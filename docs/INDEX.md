# Box map

- root box: see CONTRACT.md. Inside: src/db (index, resolve/list/write), src/gen (ComfyUI client, templates, map derivation, seam gate), src/cli, src/ui (preview: views/widgets/components), comfy/ (docker up + custom nodes), templates/ + prompts/ (workflow JSON and prompt text), themes/ (the database).
