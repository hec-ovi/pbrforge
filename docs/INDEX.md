# Box map

- root box: see CONTRACT.md. Inside: src/db (index, resolve/list/write), src/gen (ComfyUI client, templates, map derivation, the finish a photographed surface is read under, re-reading maps already in the database, screen structure and wordmark, provided source images through the 4x upscale, color maths, the stroked alphabet, tint variants, seam gate), src/gen/rebrand (the rebrand lane: naming, wordmark layout, letter atlas compositing, the writer), src/gen/pattern (the pattern class: one file per pattern, plus the sampler base, the factory and the rasterizer), src/cli (resolve, create, refinish, rebrand, contact sheets), src/ui (preview: views/widgets/components), comfy/ (docker up + custom nodes), templates/ + prompts/ (workflow JSON and prompt text), batch/ (create requests per kind, the sample business list), themes/ (the database).
