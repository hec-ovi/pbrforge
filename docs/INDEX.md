# Box map

- root box: see CONTRACT.md. Inside: src/db (index, resolve/list/write), src/gen (ComfyUI client, templates, map derivation, the finish a photographed surface is read under, re-reading maps already in the database, screen structure and wordmark, color maths, the stroked alphabet, tint variants, seam gate), src/gen/pattern (the pattern class: one file per pattern, plus the sampler base, the factory and the rasterizer), src/cli (resolve, create, refinish, contact sheets), src/ui (preview: views/widgets/components), comfy/ (docker up + custom nodes), templates/ + prompts/ (workflow JSON and prompt text), batch/ (create requests per kind), themes/ (the database).
