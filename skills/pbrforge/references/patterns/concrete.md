# concrete

Mineral clouds, cast traces, shallow pores, optional panel seams. File: `src/gen/pattern/Concrete.ts`.

`cells`, `line`, `bevel`, `depth`, `joint`, `wear`. Panel seams from the grid; pores stay physical scale independent of module size.
