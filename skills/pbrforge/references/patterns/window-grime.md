# window-grime

Fitted translucent runoff decal. File: `src/gen/pattern/WindowGrime.ts`.

Needs `decal` + `alphaMode: BLEND`. Opacity from runoff * wear * edge inset. PNG top edge is the start of downward flow. Flat relief. Opacity stays below 0.3.
