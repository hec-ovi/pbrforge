# lamp

One luminaire on the sheet. File: `src/gen/pattern/Lamp.ts`. Recipe: `batch/cyberpunk/light-fixture.json`.

`line` bezel width, `bevel` chamfer to the lens, `split` how far the hot centre reaches. Colors: lens, housing, centre. Emission comes from the lens; housing stays dark. Exact face, not a tiled wall.
