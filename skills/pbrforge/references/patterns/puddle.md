# puddle

Noise field with damp pools. File: `src/gen/pattern/PuddleField.ts`.

`wet` 0 dry, 0.5 about half flooded. Third color is the pool. Inside a pool: flat height, roughness 0.5. Mask wraps with the asphalt lattice.
