# water

Periodic waves that wrap. File: `src/gen/pattern/WaterSurface.ts`. Recipe: `batch/cyberpunk/water-surface.json`.

`cells` are whole wave cycles on the tile. `axis` main direction. `chop` crossing-direction strength. Two water colors. Depth drives normals. No emission.
