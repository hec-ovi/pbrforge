# lane

Asphalt plus two wheel tracks. File: `src/gen/pattern/LaneField.ts`. Recipe: road batches.

Everything `noise` reads, plus `axis` (lane direction), `split` (track spacing), `line` (track width), `wear` (how dark and damp the tracks are).
