{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:urbe:create-request",
  "title": "CreateRequest",
  "type": "object",
  "required": ["key", "alignment", "description"],
  "additionalProperties": false,
  "properties": {
    "key": {
      "type": "string",
      "pattern": "^[a-z0-9_-]+/[a-z0-9_-]+/[a-z0-9_-]+$",
      "description": "theme/kind/tier the new entry is written under"
    },
    "aliases": {
      "type": "array",
      "items": { "type": "string", "pattern": "^[a-z0-9_-]+/[a-z0-9_-]+/[a-z0-9_-]+$" }
    },
    "alignment": { "enum": ["tile", "exact"] },
    "description": {
      "type": "string",
      "minLength": 3,
      "description": "what the surface is, in plain words; the template turns it into the full prompt"
    },
    "brandName": {
      "type": "string",
      "minLength": 1,
      "description": "business the advertisement is for; never enters the diffusion prompt, it is stroked into the emission map afterwards, so the named world can rebrand a screen without a new render"
    },
    "businessKind": {
      "type": "string",
      "minLength": 1,
      "description": "what that business sells, in plain words (noodle bar, pawn shop, parfumery); this one does steer the artwork"
    },
    "tiling": {
      "type": "object",
      "required": ["worldSize"],
      "additionalProperties": false,
      "properties": {
        "worldSize": {
          "type": "array",
          "items": { "type": "number", "exclusiveMinimum": 0 },
          "minItems": 2,
          "maxItems": 2
        }
      }
    },
    "aspect": {
      "type": "array",
      "items": { "type": "number", "exclusiveMinimum": 0 },
      "minItems": 2,
      "maxItems": 2
    },
    "decal": { "$ref": "#/$defs/decalPlacement" },
    "physical": {
      "$comment": "same shape as material-entry physical; authored, not estimated",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "breakable": { "type": "boolean" },
        "metallicFactor": { "type": "number", "minimum": 0, "maximum": 1 },
        "roughnessFactor": { "type": "number", "minimum": 0, "maximum": 1 },
        "transmission": { "type": "number", "minimum": 0, "maximum": 1 },
        "ior": { "type": "number", "minimum": 1 },
        "tint": { "type": "string", "pattern": "^#[0-9a-f]{6}$" },
        "emissiveStrength": { "type": "number", "minimum": 0 },
        "alphaMode": { "enum": ["OPAQUE", "BLEND", "MASK"] }
      }
    },
    "finish": {
      "type": "object",
      "description": "how a photographed surface is read into relief and gloss; ignored by the pattern, flat, recolor and screen lanes, and inherited from the entry when appending",
      "additionalProperties": false,
      "properties": {
        "roughness": {
          "type": "array",
          "items": { "type": "number", "minimum": 0, "maximum": 1 },
          "minItems": 2,
          "maxItems": 2,
          "description": "[min, max] band the roughness map stays inside; default: the roughness factor plus or minus 0.05"
        },
        "grain": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "default": 0.2,
          "description": "how much of the pixel-scale speckle survives into the relief; 0 keeps feature scale alone"
        },
        "relief": {
          "type": "number",
          "minimum": 0,
          "maximum": 4,
          "default": 2,
          "description": "gain on the feature-scale relief: joints, bricks, aggregate"
        }
      }
    },
    "sourceImage": {
      "type": "object",
      "required": ["path"],
      "additionalProperties": false,
      "properties": { "path": { "type": "string", "minLength": 1 } },
      "description": "exact baked image plate; center-cover fitted locally, flat physical maps and identical basecolor and emission; source must cover the output resolution"
    },
    "flatColor": {
      "type": "string",
      "pattern": "^#[0-9a-f]{6}$",
      "description": "when set, the basecolor is synthesized procedurally from this color (glass, plain colors) instead of generated via ComfyUI"
    },
    "flatNoise": {
      "type": "number",
      "minimum": 0,
      "maximum": 0.2,
      "default": 0.04,
      "description": "noise amplitude for flatColor synthesis"
    },
    "pattern": {
      "type": "object",
      "description": "procedural lane: the surface is drawn from these parameters instead of photographed, crisp and tileable by construction",
      "required": ["kind", "colors"],
      "additionalProperties": false,
      "properties": {
        "kind": { "enum": ["concrete", "hexagon", "panel-grid", "slab", "stripe", "two-tone", "noise", "lane", "puddle", "lamp", "glyph-atlas", "grille", "water", "incident-blood", "incident-tyre"] },
        "colors": {
          "type": "array",
          "items": { "type": "string", "pattern": "^#[0-9a-f]{6}$" },
          "minItems": 1,
          "maxItems": 3,
          "description": "face color first, then the band color and the trim color for the kinds that take them"
        },
        "cells": {
          "type": "array",
          "items": { "type": "integer", "minimum": 1 },
          "minItems": 2,
          "maxItems": 2,
          "description": "cells across one tile; for hexagon, columns and staggered row pairs (x about 1.73 times y keeps them regular)"
        },
        "line": { "type": "number", "minimum": 0, "description": "joint or edge line width, in metres; a lane's wheel track width, a lamp's bezel width, a grille's ring pitch" },
        "bevel": { "type": "number", "minimum": 0, "description": "chamfer width beside a joint, from a lamp's bezel down to its lens, or a grille's flange width, in metres" },
        "depth": { "type": "number", "minimum": 0, "maximum": 1, "description": "relief between a face and a joint" },
        "joint": { "type": "number", "minimum": 0, "maximum": 1, "description": "how much darker and rougher a joint reads" },
        "variation": { "type": "number", "minimum": 0, "maximum": 1, "description": "tone spread from one cell to the next" },
        "sheen": { "type": "number", "minimum": 0, "maximum": 1, "description": "gloss spread from one cell to the next: a printed pattern" },
        "grain": { "type": "number", "minimum": 0, "maximum": 0.2, "description": "fine tonal mottling over everything" },
        "octaves": { "type": "integer", "minimum": 1, "maximum": 4, "description": "octaves of a noise field: one is a plain wall, four is asphalt" },
        "wet": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "puddle only: how much of the surface stands under water; 0 is a dry road, 0.5 floods about half of it"
        },
        "wear": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "lane: track wear; grille: housing dirt; concrete: casting traces and localized mineral stains; 0 is unworn"
        },
        "chop": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "water only: strength of crossing wave directions; 0 is parallel and 1 is choppy"
        },
        "bond": { "enum": ["stack", "running"], "description": "running shifts every other row half a cell; needs an even row count" },
        "axis": { "enum": ["x", "y"], "description": "which way stripe bands, the two-tone split and a lane run" },
        "split": { "type": "number", "minimum": 0, "maximum": 1, "description": "where the band boundary sits, as a fraction of the tile; a lane's track spacing across it; how far a lamp's hot centre reaches over its lens; a grille's diameter over the face" }
      }
    },
    "layout": {
      "$ref": "#/$defs/surfaceLayout",
      "description": "published visible divisions in world metres; independent of fine material grain"
    },
    "recolor": {
      "type": "object",
      "description": "tint lane: another variant of the entry this one is appended to, repainted",
      "required": ["from", "color"],
      "additionalProperties": false,
      "properties": {
        "from": { "type": "string", "pattern": "^[a-z0-9_-]+$", "description": "the variant id the tint is made from" },
        "color": { "type": "string", "pattern": "^#[0-9a-f]{6}$", "description": "the paint: its hue is taken whole" },
        "strength": { "type": "number", "minimum": 0, "maximum": 1, "description": "how much pigment is in the paint" },
        "value": { "type": "number", "minimum": 0, "maximum": 4, "description": "brightness multiplier" }
      }
    },
    "emission": {
      "enum": ["none", "luminance", "color-mask", "image"],
      "default": "none",
      "description": "how the emission map is made: derived from the base color, or `image` for a screen, where ComfyUI paints the artwork straight into the emission map and flatColor becomes the dark display glass"
    },
    "screens": {
      "type": "array",
      "minItems": 1,
      "maxItems": 8,
      "description": "screen lane only: one display per variant, in order; its length sets the variant count",
      "items": {
        "type": "object",
        "required": ["kind", "description"],
        "additionalProperties": false,
        "properties": {
          "kind": {
            "enum": ["led-dot", "scanline-billboard", "glyph-panel"],
            "description": "led-dot and scanline-billboard show photographic ad artwork through their pixel structure; glyph-panel is an abstract glowing panel"
          },
          "description": { "type": "string", "minLength": 3, "description": "the advertisement itself, as flat brandless artwork; what the source shows when imagePath is set" },
          "imagePath": {
            "type": "string",
            "minLength": 1,
            "description": "a provided source image, relative to the box folder, used as the artwork instead of diffusing one; it goes through a ComfyUI 4x upscale and then the same screen treatment"
          },
          "brandName": { "type": "string", "minLength": 1, "description": "overrides the request brandName for this screen" },
          "businessKind": { "type": "string", "minLength": 1, "description": "overrides the request businessKind for this screen" },
          "pitch": { "type": "integer", "minimum": 2, "description": "pixels per LED cell or scan band; default: width/160" }
        }
      }
    },
    "variants": { "type": "integer", "minimum": 1, "maximum": 8, "default": 1 },
    "variantId": {
      "type": "string",
      "pattern": "^[a-z0-9_-]+$",
      "description": "id and asset folder for the variant; default: its position in the entry"
    },
    "append": {
      "type": "boolean",
      "default": false,
      "description": "add the variant to the existing entry, which keeps its alignment, tiling and physical, instead of writing a new entry"
    },
    "canonical": {
      "type": "boolean",
      "default": false,
      "description": "with append: the variant becomes variant 0, the one a consumer gets when it does not pick"
    },
    "seed": { "type": "integer", "minimum": 0, "description": "default: derived from the description" },
    "resolution": {
      "type": "array",
      "items": { "type": "integer", "minimum": 64, "maximum": 4096 },
      "minItems": 2,
      "maxItems": 2,
      "default": [1024, 1024],
      "description": "[px width, px height]; must fit the physical tile or exact aspect within one pixel, with at most 1,048,576 pixels for a tile or 9,437,184 pixels for an exact sheet"
    },
    "overwrite": { "type": "boolean", "default": false }
  },
  "$defs": {
    "decalPlacement": {
      "type": "object",
      "required": ["worldSize", "edgeInset", "surfaceOffset", "wrapMode", "projection"],
      "additionalProperties": false,
      "properties": {
        "worldSize": {
          "type": "array",
          "items": { "type": "number", "exclusiveMinimum": 0 },
          "minItems": 2,
          "maxItems": 2
        },
        "edgeInset": { "type": "number", "exclusiveMinimum": 0 },
        "surfaceOffset": { "type": "number", "exclusiveMinimum": 0, "maximum": 0.01 },
        "wrapMode": { "const": "clamp" },
        "projection": { "const": "surface-fit" }
      }
    },
    "surfaceLayout": {
      "type": "object",
      "required": ["family", "origin", "orientation"],
      "additionalProperties": false,
      "properties": {
        "family": { "enum": ["continuous", "panel", "band", "lane"] },
        "moduleSize": {
          "type": "array",
          "items": { "type": "number", "exclusiveMinimum": 0 },
          "minItems": 2,
          "maxItems": 2
        },
        "jointWidth": { "type": "number", "minimum": 0 },
        "origin": {
          "type": "array",
          "items": { "type": "number" },
          "minItems": 2,
          "maxItems": 2
        },
        "orientation": { "enum": ["horizontal", "vertical", "isotropic"] },
        "bandHeight": { "type": "number", "exclusiveMinimum": 0 }
      },
      "allOf": [
        {
          "if": { "properties": { "family": { "const": "panel" } } },
          "then": { "required": ["moduleSize", "jointWidth"] }
        },
        {
          "if": { "properties": { "family": { "const": "band" } } },
          "then": { "required": ["bandHeight"] }
        }
      ]
    }
  },
  "allOf": [{
    "if": { "required": ["sourceImage"] },
    "then": {
      "properties": {
        "alignment": { "const": "exact" },
        "variants": { "const": 1 },
        "emission": { "const": "none" },
        "resolution": { "type": "array", "items": { "type": "integer", "maximum": 1024 } }
      },
      "not": { "anyOf": [
        { "required": ["pattern"] }, { "required": ["flatColor"] },
        { "required": ["recolor"] }, { "required": ["screens"] },
        { "required": ["finish"] }, { "required": ["tiling"] },
        { "required": ["decal"] }, { "required": ["layout"] },
        { "required": ["brandName"] }, { "required": ["businessKind"] }
      ] }
    }
  }],
  "if": {
    "properties": { "emission": { "const": "image" } },
    "required": ["emission"]
  },
  "then": { "required": ["flatColor", "screens"] }
}
