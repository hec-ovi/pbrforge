{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:urbe:rebrand-request",
  "title": "RebrandRequest",
  "description": "the businesses of a named world, each to be advertised on the screens of its tier",
  "type": "array",
  "minItems": 1,
  "items": {
    "type": "object",
    "required": [
      "brandName",
      "businessKind",
      "tier"
    ],
    "additionalProperties": false,
    "properties": {
      "brandName": {
        "type": "string",
        "minLength": 1,
        "maxLength": 32,
        "description": "the wordmark as it reads on the screen; uppercased and spelled from the letter atlas, so it stays inside the atlas charset"
      },
      "businessKind": {
        "enum": [
          "hotel",
          "commerce",
          "mall",
          "restaurant",
          "coffee_shop",
          "corpo",
          "clinic"
        ],
        "description": "the parcel type that advertises; with the name, it picks which artwork of the tier the brand lands on"
      },
      "tier": {
        "enum": [
          "poor",
          "mid",
          "rich",
          "high_rich"
        ]
      }
    }
  }
}
